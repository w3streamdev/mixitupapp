<Window x:Class="MixItUp.Installer.EulaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:markdown="clr-namespace:MdXaml;assembly=MdXaml"
        Title="Mix It Up End User License Agreement"
        Height="600" Width="600"
        MinHeight="400" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Please review the Mix It Up End User License Agreement."
                   TextWrapping="Wrap"
                   Margin="0,0,0,4"/>

    <markdown:MarkdownScrollViewer Grid.Row="2"
                       x:Name="EulaViewer"/>

        <Border Grid.Row="2"
                x:Name="FallbackBorder"
                Visibility="Collapsed"
                BorderThickness="1"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="FallbackText"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OpenInBrowserButton"
                    Content="Open in Browser"
                    Margin="0,0,10,0"
                    Click="OpenInBrowserButton_Click"
                    Visibility="Collapsed"/>
            <Button x:Name="DeclineButton"
                    Content="Decline"
                    IsCancel="True"
                    Margin="0,0,10,0"
                    Click="DeclineButton_Click"/>
            <Button x:Name="AcceptButton"
                    Content="Accept"
                    IsDefault="True"
                    Click="AcceptButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
