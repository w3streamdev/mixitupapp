<Controls:LoadingControlBase x:Class="MixItUp.WPF.Controls.Services.ServiceCategoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MixItUp.WPF.Controls"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox x:Name="CategoryGroupBox">
        <GroupBox.Header>
            <Grid MouseLeftButtonUp="CategoryGroupBoxHeader_MouseLeftButtonUp">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}" Text="{Binding CategoryName}" />
                <Rectangle Grid.Column="1" Fill="{DynamicResource MaterialDesign.Brush.Primary}" />
                <MaterialDesign:PackIcon Grid.Column="2" Kind="ChevronDown" Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}" x:Name="ExpandIcon" Height="20" Width="20" />
            </Grid>
        </GroupBox.Header>
        <StackPanel x:Name="ServicesPanel" />
    </GroupBox>
</Controls:LoadingControlBase>